releases:
  archlinux:
    base_dir: archlinux
    enabled: true
    menu: linux
    mirror: mirrors.kernel.org
    name: Arch Linux
    versions:
    - code_name: {{ version }}
      name: {{ version }}

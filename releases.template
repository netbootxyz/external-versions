releases:
  archlinux:
    base_dir: archlinux
    enabled: true
    menu: linux
    mirror: mirror.rackspace.com
    name: Arch Linux
    versions:
    - code_name: {{ version }}
      name: {{ version }}

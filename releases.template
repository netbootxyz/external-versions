releases:
  nixos:
    base_dir: nixos
    enabled: true
    menu: linux
    name: NixOS
    versions:
{% set versions = version.split(';') %}
{% for value in versions %}
    - code_name: {{ value }}
      name: {{ value }}
{% endfor %}

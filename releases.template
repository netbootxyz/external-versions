releases:
  archlinux:
    base_dir: archlinux
    enabled: true
    menu: linux
    mirror: mirrors.evowise.com
    name: Arch Linux
    versions:
    - code_name: {{ version }}
      name: {{ version }}
